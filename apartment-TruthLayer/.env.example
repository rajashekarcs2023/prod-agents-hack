# Parallel Web API Configuration
VITE_PARALLEL_API_KEY=your_parallel_api_key_here

# Lightpanda Cloud Configuration (Optional - for cloud browser)
VITE_LPD_TOKEN=your_lightpanda_token_here

# EmailJS Configuration (Optional - for email notifications)
VITE_EMAILJS_SERVICE_ID=your_emailjs_service_id
VITE_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key
VITE_USER_EMAIL=rajashekarvennavelli@gmail.com

# Webhook Configuration (Optional - for custom integrations)
VITE_WEBHOOK_URL=https://your-webhook-endpoint.com/api/webhooks

# Google AI (Gemini) API Key (Optional - for fallback)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Setup Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual API keys
# 3. At minimum, add VITE_PARALLEL_API_KEY for real data gathering
# 4. For email notifications, sign up at https://emailjs.com and add EmailJS keys
# 5. The app works in demo mode without any API keys, but with mock data

# EmailJS Setup (2 minutes):
# 1. Go to https://emailjs.com → Sign up (free)
# 2. Connect your Gmail/Outlook
# 3. Create email template with these variables: {{to_email}}, {{subject}}, {{listing_address}}, {{truth_score}}, {{summary}}
# 4. Get Service ID, Template ID, and Public Key → Add above